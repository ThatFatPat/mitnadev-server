<div id="NavBar" dir="ltr">
<nav class="navbar navbar-dark bg-dark navbar-fixed-top navbar-expand heb">
  <div class="navbar-header">
    <a href="/" class="navbar-brand"><img height="30" width="30" src="/assets/logo.png"></a>
  </div>
  <div class="collapse navbar-collapse login" id="content">
    <ul class="navbar-nav">
      <% if (!user) { %>
      <li class="nav-item active">
        <a class="nav-link unselectable" unselectable="on" href="/login" > התחבר </a>
      </li>
      <% } else { %> 
      <li class="nav-item dropdown active heb">
        <a class="nav-link dropdown-toggle" id="userDropdown" data-toggle="dropdown">ברוכים הבאים,  <%= user.name %></a>
        <div class="dropdown-menu" aria-labelledby="userDropdown">
        <% if (user.type === 0) { %>
          <a class="dropdown-item" href="/">תלמיד</a>
        <% } else if (user.type === 1) { %>
          <a class="dropdown-item" href="/">מורה</a>
        <% } else if (user.type === 2) { %>
          <a class="dropdown-item" href="/">מנהל</a>
        <% } %>
          <div class="dropdown-divider"></div>
          <a class="dropdown-item" href="/settings">הגדרות</a>
          <a class="dropdown-item" style="cursor: pointer" href="/logout">התנתקות</a>
        </div>
      </li>
      <% } %>
    </ul>
  </div>
</nav>
</div>