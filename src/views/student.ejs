<!DOCTYPE html>
<html>
    <head>
        <% include ../templates/head %>
        <link rel="stylesheet" href="/static/table.css">
        <script>
            function replaceActive() {
                for(let x of $("[key=active]")){
                    if (x.innerHTML.trim() === '1'){
                    //V
                        x.innerHTML = '✔️'
                    } else if (x.innerHTML.trim() === '0') {
                    //X
                        x.innerHTML = '❌'
                    }
                
                }                                    
            }
            $(document).ready(replaceActive)
        </script>
    </head>
    <body>
        <% include ../templates/navbar %>
        <%- include('../templates/table', {tableid: "subjecttable", visible: "hidden", headers: headers, data: matches, button: false, button_text:'', radio: false, add: false}); %>
    </body>
</html>